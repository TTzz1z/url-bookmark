# SQLite 数据固定保存在项目根目录的 data/bookmarks.db。
# 仅在测试或特殊本地环境中覆盖此路径。
DATABASE_PATH=./data/bookmarks.db

# 抓取限制（毫秒、字节、重定向次数）
FETCH_TIMEOUT_MS=12000
FETCH_MAX_BYTES=5242880
FETCH_MAX_REDIRECTS=5

# 仅供自动化测试显式开启；正常运行必须保持为 0。
ALLOW_TEST_LOOPBACK=0
